To use the Documentation, install DocFx:

dotnet tool install -g docfx

Then navigate to into the Documentaion Folder in you command window:

cd ..\..\im-blocks\Documentation

Execute following command:

docfx docfx.json --serve

The API Documentation will run on your localhost, so open any browser and excess your localhost via:

localhost:8080
